# Optional. If these are absent, the app falls back to local workbook tools.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional custom AI-compatible endpoint. When set, /api/ai forwards the workbook payload here.
AI_API_URL=
AI_API_KEY=
AI_MODEL=
